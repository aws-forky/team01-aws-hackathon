# ===================================================================
# ## .gitignore 종합 예시 ##
# 주석은 '#'으로 시작합니다. 이 파일은 Git이 추적하지 말아야 할
# 파일 및 폴더 목록을 정의합니다.
# ===================================================================

# -------------------------------------------------------------------
# 운영체제(OS) 및 시스템 파일
# OS가 자동으로 생성하는 시스템 파일들입니다.
# -------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
._*
.Spotlight-V100
.Trashes

# -------------------------------------------------------------------
# IDE 및 코드 에디터 설정 파일
# 개인의 개발 환경 설정이 팀원과 공유될 필요는 없습니다.
# -------------------------------------------------------------------
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
.history

# -------------------------------------------------------------------
# 로그, 캐시, 임시 파일
# 실행 중 동적으로 생성되며, 버전 관리가 필요 없는 파일들입니다.
# -------------------------------------------------------------------
*.log
log/
logs/
tmp/
temp/
cache/
*.cache

# -------------------------------------------------------------------
# 의존성(Dependency) 폴더
# 패키지 매니저를 통해 설치되는 라이브러리 폴더입니다.
# 용량이 매우 크고, `package.json`이나 `requirements.txt`를 통해
# 다른 환경에서도 동일하게 설치할 수 있으므로 절대 포함하지 않습니다.
# -------------------------------------------------------------------
node_modules/
bower_components/
jspm_packages/

# Python 관련
venv/
env/
.env/
__pycache__/
*.pyc

# -------------------------------------------------------------------
# 빌드(Build) 결과물
# 소스 코드를 컴파일하거나 빌드할 때 생성되는 산출물입니다.
# -------------------------------------------------------------------
build/
dist/
out/
target/
*.o
*.obj

# -------------------------------------------------------------------
# 민감한 정보 및 환경 변수 파일
# API 키, 데이터베이스 접속 정보 등 절대 외부에 노출되면 안 되는
# 중요한 정보를 담은 파일입니다. 보안상 필수 항목입니다.
# -------------------------------------------------------------------
.env
.env.*
!.env.example
config/credentials.yml

# -------------------------------------------------------------------
# 기타
# 압축 파일, 데이터 파일 등
# -------------------------------------------------------------------
*.zip
*.tar
*.gz
*.csv
*.sql
